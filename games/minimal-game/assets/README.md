# Minimal game assets

Assets are split by runtime responsibility:

- `logic/` contains authoritative data available to both client and server.
- `presentation/` contains client-only visual, audio, and UI data.

The asset provider and packaging pipeline have not been selected yet.

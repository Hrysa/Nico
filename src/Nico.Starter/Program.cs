using Microsoft.Extensions.Hosting;
using Nico.Core;

// HostApplicationBuilder builder = Host.CreateApplicationBuilder(args);
// builder.Services.AddCreatorHostedService();

// using var host = builder.Build();
// await host.RunAsync();

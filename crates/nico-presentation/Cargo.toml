[package]
name = "nico-presentation"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = ["runtime"]
runtime = ["dep:nico-runtime"]

[dependencies]
glam = "0.30"
nico-assets = { path = "../nico-assets" }
nico-runtime = { path = "../nico-runtime", optional = true }

[lints]
workspace = true
